# 📱 Подробный разбор Android приложения Luggify

## Содержание
1. [Общая информация о приложении](#общая-информация-о-приложении)
2. [Архитектура приложения](#архитектура-приложения)
3. [Структура проекта](#структура-проекта)
4. [Детальный разбор файлов](#детальный-разбор-файлов)
5. [Поток данных](#поток-данных)
6. [Технологии и библиотеки](#технологии-и-библиотеки)
7. [Вопросы для защиты](#вопросы-для-защиты)

---

## Общая информация о приложении

**Luggify** — это Android-приложение для генерации чеклистов вещей для путешествий на основе выбранного города и дат поездки.

### Основные возможности:
1. **Поиск городов** — автодополнение с помощью API
2. **Выбор дат** — выбор даты отправления и возвращения с валидацией
3. **Генерация чеклиста** — автоматическое создание списка вещей на основе погоды и города
4. **Управление чеклистом** — отметка выполненных вещей, удаление, добавление новых
5. **Сохранение чеклистов** — сохранение в "Мои чеклисты" для последующего использования
6. **Сохранение настроек** — автоматическое сохранение выбранного города и дат

---

## Архитектура приложения

Приложение использует **MVVM (Model-View-ViewModel)** архитектуру:

```
┌─────────────────────────────────────────┐
│           UI Layer (Compose)            │
│  ┌──────────┐  ┌──────────┐  ┌───────┐ │
│  │  Screens │  │Components│  │ Theme │ │
│  └────┬─────┘  └────┬─────┘  └───────┘ │
└───────┼─────────────┼──────────────────┘
        │             │
        ▼             ▼
┌─────────────────────────────────────────┐
│         ViewModel Layer                 │
│  ┌───────────────────────────────────┐ │
│  │     LuggifyViewModel              │ │
│  │  - Управляет состоянием UI         │ │
│  │  - Связывает UI и Repository      │ │
│  └───────────────┬────────────────────┘ │
└──────────────────┼──────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────┐
│         Data Layer                       │
│  ┌─────────────┐  ┌──────────────┐     │
│  │ Repository  │  │ DataStore    │     │
│  └──────┬───────┘  └──────────────┘     │
│         │                                │
│         ▼                                │
│  ┌─────────────┐  ┌──────────────┐     │
│  │    API      │  │   Models     │     │
│  └─────────────┘  └──────────────┘     │
└─────────────────────────────────────────┘
```

---

## Структура проекта

```
android-app/app/src/main/java/com/luggify/app/
├── MainActivity.kt                    # Точка входа, навигация
├── data/
│   ├── api/
│   │   ├── ApiClient.kt             # Настройка Retrofit
│   │   └── LuggifyApi.kt            # Интерфейс API
│   ├── datastore/
│   │   └── UserPreferencesDataStore.kt  # Сохранение настроек
│   ├── models/
│   │   ├── City.kt                   # Модель города
│   │   └── Checklist.kt             # Модели чеклиста
│   └── repository/
│       └── LuggifyRepository.kt     # Работа с API
├── ui/
│   ├── components/
│   │   ├── CitySelect.kt            # Компонент выбора города
│   │   ├── DateRangePicker.kt      # Компонент выбора дат
│   │   ├── ChecklistItem.kt        # Элемент чеклиста
│   │   └── WeatherForecast.kt      # Прогноз погоды
│   ├── screens/
│   │   ├── HomeScreen.kt            # Главный экран
│   │   ├── ChecklistScreen.kt      # Экран чеклиста
│   │   └── MyChecklistsScreen.kt   # Сохраненные чеклисты
│   ├── theme/
│   │   ├── Color.kt                 # Цветовая схема
│   │   ├── Theme.kt                # Тема приложения
│   │   └── Type.kt                  # Типографика
│   └── viewmodel/
│       └── LuggifyViewModel.kt      # ViewModel с логикой
└── utils/
    └── UserIdHelper.kt              # Получение ID пользователя
```

---

## Детальный разбор файлов

### 1. MainActivity.kt

**Назначение:** Точка входа в приложение, настройка навигации и инициализация компонентов.

#### Структура:

```kotlin
class MainActivity : ComponentActivity() {
    // Переопределяем метод создания активности
    override fun onCreate(savedInstanceState: Bundle?) {
        // Вызываем родительский метод для базовой инициализации
        super.onCreate(savedInstanceState)
        
        // 1. Получаем уникальный ID пользователя через helper
        // this - это контекст Activity, нужен для доступа к системным настройкам
        val userId = UserIdHelper.getUserId(this)
        
        // 2. Создаем экземпляр DataStore для сохранения настроек пользователя
        // this передается как Context для доступа к файловой системе
        val userPreferencesDataStore = UserPreferencesDataStore(this)
        
        // 3. Создаем ViewModel с зависимостями
        // userId - для идентификации пользователя при сохранении чеклистов
        // userPreferencesDataStore - для сохранения/загрузки настроек
        val viewModel = LuggifyViewModel(
            userId = userId,
            userPreferencesDataStore = userPreferencesDataStore
        )
        
        // 4. Устанавливаем Compose UI вместо XML layout
        // setContent - функция для установки корневого Compose контента
        setContent {
            // Применяем тему приложения (цвета, типографика)
            LuggifyTheme {
                // Основная функция приложения с навигацией
                LuggifyApp(viewModel = viewModel)
            }
        }
    }
}
```

#### Функция `LuggifyApp`:

**Назначение:** Настройка навигации между экранами.

```kotlin
@Composable
fun LuggifyApp(viewModel: LuggifyViewModel) {
    // Создаем контроллер навигации (запоминается между рекомпозициями)
    // rememberNavController - создает NavController для управления навигацией
    // remember - сохраняет значение между рекомпозициями (не пересоздается)
    val navController = rememberNavController()

    // NavHost - контейнер для всех экранов приложения
    // Это корневой компонент навигации
    NavHost(
        navController = navController,  // Передаем контроллер навигации
        startDestination = "home"       // Начальный экран при запуске приложения
    ) {
        // Определяем маршрут для главного экрана
        // composable - определяет один маршрут (экран)
        composable("home") {
            // Отображаем главный экран
            HomeScreen(
                viewModel = viewModel,  // Передаем ViewModel для доступа к данным
                // Callback для перехода на экран чеклиста
                // slug - уникальный идентификатор чеклиста
                onNavigateToChecklist = { slug ->
                    // Переходим на экран чеклиста с параметром slug
                    // navigate - выполняет переход на указанный маршрут
                    navController.navigate("checklist/$slug") {
                        // popUpTo - очищает стек навигации до указанного маршрута
                        // Это означает, что если нажать "Назад", вернемся на "home"
                        popUpTo("home") { 
                            inclusive = false  // "home" остается в стеке (можно вернуться назад)
                            // Если бы было inclusive = true, "home" бы удалился из стека
                        }
                    }
                },
                // Callback для перехода на экран "Мои чеклисты"
                onNavigateToMyChecklists = {
                    navController.navigate("my-checklists") {
                        popUpTo("home") { inclusive = false }
                    }
                }
            )
        }
        
        // Маршрут с параметром {slug} - динамическая часть пути
        // {slug} будет заменен на реальное значение при навигации
        composable("checklist/{slug}") { backStackEntry ->
            // Извлекаем параметр slug из URL
            // backStackEntry - информация о текущем маршруте
            // backStackEntry.arguments - содержит параметры маршрута
            // ?.getString("slug") - получаем строковое значение параметра "slug"
            // ?: return@composable - если slug не найден, выходим из функции
            val slug = backStackEntry.arguments?.getString("slug") ?: return@composable
            
            // Отображаем экран чеклиста
            ChecklistScreen(
                slug = slug,  // Передаем slug для загрузки чеклиста
                viewModel = viewModel,
                onNavigateBack = {
                    // Возвращаемся на предыдущий экран
                    // popBackStack() - возвращает на предыдущий экран в стеке
                    navController.popBackStack()
                    // Очищаем состояние чеклиста в ViewModel
                    // Это нужно, чтобы при следующем открытии загружался новый чеклист
                    viewModel.navigateBack()
                }
            )
        }
        
        // Маршрут для экрана "Мои чеклисты"
        composable("my-checklists") {
            MyChecklistsScreen(
                viewModel = viewModel,
                onNavigateBack = {
                    // Просто возвращаемся назад
                    navController.popBackStack()
                },
                onNavigateToChecklist = { slug ->
                    // Переходим на чеклист из списка
                    navController.navigate("checklist/$slug") {
                        // Очищаем стек до "my-checklists"
                        // Это означает, что если нажать "Назад" с чеклиста, вернемся на "my-checklists"
                        popUpTo("my-checklists") { inclusive = false }
                    }
                }
            )
        }
    }
}
```

**Что делает:**
- Создает `NavController` для управления навигацией
- Настраивает маршруты:
  - `"home"` — главный экран
  - `"checklist/{slug}"` — экран чеклиста (slug передается как параметр)
  - `"my-checklists"` — список сохраненных чеклистов

**Важные моменты:**
- `popUpTo("home")` — очищает стек навигации до главного экрана
- `inclusive = false` — главный экран остается в стеке (можно вернуться назад)

---

### 2. data/models/ — Модели данных

#### City.kt

```kotlin
// data class - автоматически генерирует equals(), hashCode(), toString(), copy()
// Используется для хранения данных (неизменяемые объекты)
data class City(
    val name: String,        // Название города (например, "Москва")
    val country: String,     // Страна (например, "Россия")
    val lat: Double,         // Широта (географическая координата)
    val lon: Double,         // Долгота (географическая координата)
    val fullName: String     // Полное название (например, "Москва, Россия")
    // fullName используется для отображения в UI и отправки на сервер
)
```

**Использование:** Хранение информации о городе, полученной из API автодополнения.

#### Checklist.kt

Содержит несколько data классов:

**Checklist** — основная модель чеклиста:
```kotlin
// data class для хранения данных чеклиста
// Все поля соответствуют структуре JSON ответа от сервера
data class Checklist(
    val slug: String,                    // Уникальный идентификатор (например, "abc123")
    val city: String,                     // Название города (например, "Москва, Россия")
    val start_date: String,               // Дата начала в формате "yyyy-MM-dd" (например, "2024-01-15")
    val end_date: String,                 // Дата окончания в формате "yyyy-MM-dd"
    val items: List<String>,              // Список вещей (оригинальный список от сервера)
    val checked_items: List<String>?,    // Отмеченные вещи (null если нет отмеченных)
    val removed_items: List<String>?,     // Удаленные вещи (null если нет удаленных)
    val added_items: List<String>?,       // Добавленные пользователем вещи (null если нет добавленных)
    val daily_forecast: List<DailyForecast>?, // Прогноз погоды на дни поездки (null если нет прогноза)
    val items_by_category: Map<String, List<String>>? = null,  // Вещи по категориям (опционально)
    val avg_temp: Double? = null,         // Средняя температура (опционально)
    val conditions: List<String>? = null  // Условия погоды (опционально)
)
```

**PackingRequest** — запрос на генерацию чеклиста:
```kotlin
// data class для отправки запроса на генерацию чеклиста
// Отправляется как JSON в теле POST запроса
data class PackingRequest(
    val city: String,        // Полное название города (например, "Москва, Россия")
    val start_date: String,  // Дата начала в формате "yyyy-MM-dd"
    val end_date: String     // Дата окончания в формате "yyyy-MM-dd"
)
```

**ChecklistStateUpdate** — обновление состояния чеклиста:
```kotlin
// data class для обновления состояния чеклиста на сервере
// Отправляется как JSON в теле PATCH запроса
data class ChecklistStateUpdate(
    val checked_items: List<String>?,  // Список отмеченных вещей (null если не обновляется)
    val removed_items: List<String>?,  // Список удаленных вещей (null если не обновляется)
    val added_items: List<String>?,    // Список добавленных вещей (null если не обновляется)
    val items: List<String>?           // Полный список вещей (для сохранения порядка)
)
```

**DailyForecast** — прогноз погоды на день:
```kotlin
// data class для хранения прогноза погоды на один день
data class DailyForecast(
    val date: String,        // Дата в формате "yyyy-MM-dd" (например, "2024-01-15")
    val temp_min: Double,   // Минимальная температура в градусах Цельсия
    val temp_max: Double,   // Максимальная температура в градусах Цельсия
    val condition: String,  // Описание условий (например, "Облачно с прояснениями")
    val icon: String        // Код иконки погоды (например, "04d" для OpenWeatherMap)
)
```

**ChecklistCreate** — создание чеклиста для сохранения:
```kotlin
// data class для создания нового чеклиста при сохранении
data class ChecklistCreate(
    val city: String,                     // Название города
    val start_date: String,               // Дата начала
    val end_date: String,                 // Дата окончания
    val items: List<String>,              // Список вещей
    val avg_temp: Double? = null,         // Средняя температура (опционально)
    val conditions: List<String>? = null, // Условия (опционально)
    val checked_items: List<String>? = null,  // Отмеченные вещи
    val removed_items: List<String>? = null,  // Удаленные вещи
    val added_items: List<String>? = null,    // Добавленные вещи
    val tg_user_id: String                // ID пользователя для сохранения
)
```

---

### 3. data/api/ — Работа с API

#### ApiClient.kt

**Назначение:** Настройка HTTP-клиента и Retrofit.

```kotlin
// object - это singleton (один экземпляр на все приложение)
object ApiClient {
    // Базовый URL сервера (константа, не изменяется)
    private const val BASE_URL = "https://luggify.onrender.com/"
    
    // Настройка перехватчика для логирования HTTP-запросов
    // HttpLoggingInterceptor - перехватывает запросы/ответы для логирования
    private val loggingInterceptor = HttpLoggingInterceptor().apply {
        // Level.BODY - логирует заголовки и тело запроса/ответа
        // Полезно для отладки, но в продакшене лучше Level.NONE
        level = HttpLoggingInterceptor.Level.BODY
    }
    
    // Настройка HTTP-клиента OkHttp
    // OkHttpClient - низкоуровневый HTTP-клиент для выполнения запросов
    private val okHttpClient = OkHttpClient.Builder()
        .addInterceptor(loggingInterceptor)    // Добавляем логирование каждого запроса
        .connectTimeout(60, TimeUnit.SECONDS)  // Максимальное время ожидания подключения (60 сек)
        .readTimeout(60, TimeUnit.SECONDS)     // Максимальное время чтения ответа (60 сек)
        .writeTimeout(60, TimeUnit.SECONDS)     // Максимальное время отправки запроса (60 сек)
        .retryOnConnectionFailure(true)         // Автоматически повторять запрос при ошибке сети
        .build()                                // Создаем финальный клиент
    
    // Настройка Retrofit - библиотека для упрощения работы с HTTP API
    private val retrofit = Retrofit.Builder()
        .baseUrl(BASE_URL)                      // Базовый URL, к которому добавляются пути из интерфейса
        .client(okHttpClient)                   // Используем настроенный OkHttpClient
        .addConverterFactory(GsonConverterFactory.create()) // Конвертер JSON <-> Kotlin объекты
        .build()                                // Создаем экземпляр Retrofit
    
    // Создаем реализацию интерфейса LuggifyApi
    // Retrofit автоматически создает класс, реализующий все методы интерфейса
    val api: LuggifyApi = retrofit.create(LuggifyApi::class.java)
}
```

**Объяснение:**
- `OkHttpClient` — HTTP-клиент для выполнения запросов
- `HttpLoggingInterceptor` — логирует все запросы/ответы (для отладки)
- `GsonConverterFactory` — автоматически конвертирует JSON в Kotlin объекты
- `retrofit.create()` — создает реализацию интерфейса `LuggifyApi`

#### LuggifyApi.kt

**Назначение:** Интерфейс с описанием всех API-эндпоинтов.

```kotlin
// Интерфейс определяет все API-эндпоинты сервера
// Retrofit автоматически создает реализацию этого интерфейса
interface LuggifyApi {
    // Поиск городов по части названия
    @GET("geo/cities-autocomplete")  // HTTP GET запрос к /geo/cities-autocomplete
    suspend fun getCities(           // suspend - асинхронная функция (корутина)
        @Query("namePrefix") namePrefix: String  // @Query - параметр в URL: ?namePrefix=Москва
    ): Response<List<City>>          // Response содержит статус код и данные
    
    // Генерация нового чеклиста
    @POST("generate-packing-list")  // HTTP POST запрос
    suspend fun generatePackingList(
        @Body request: PackingRequest  // @Body - тело запроса в формате JSON
    ): Response<Checklist>            // Возвращает созданный чеклист
    
    // Получение существующего чеклиста по уникальному идентификатору
    @GET("checklist/{slug}")          // {slug} - динамическая часть пути
    suspend fun getChecklist(
        @Path("slug") slug: String     // @Path - заменяет {slug} в пути URL
    ): Response<Checklist>            // Например: /checklist/abc123
    
    // Обновление состояния чеклиста (отмеченные/удаленные/добавленные вещи)
    @PATCH("checklist/{slug}/state")  // HTTP PATCH - частичное обновление
    suspend fun updateChecklistState(
        @Path("slug") slug: String,    // slug в пути URL
        @Body state: ChecklistStateUpdate  // Новое состояние в теле запроса
    ): Response<Checklist>            // Возвращает обновленный чеклист
    
    // Удаление чеклиста
    @DELETE("checklist/{slug}")       // HTTP DELETE запрос
    suspend fun deleteChecklist(
        @Path("slug") slug: String     // slug в пути
    ): Response<Unit>                 // Unit - нет тела ответа (только статус)
    
    // Получение всех сохраненных чеклистов пользователя
    @GET("tg-checklists/{user_id}")   // {user_id} - динамическая часть
    suspend fun getMyChecklists(
        @Path("user_id") userId: String  // ID пользователя в пути
    ): Response<List<Checklist>>      // Список чеклистов
    
    // Сохранение чеклиста в "Мои чеклисты"
    @POST("save-tg-checklist")       // HTTP POST запрос
    suspend fun saveChecklist(
        @Body checklist: ChecklistCreate  // Данные чеклиста в теле запроса
    ): Response<Checklist>            // Возвращает сохраненный чеклист
}
```

**Объяснение аннотаций:**
- `@GET`, `@POST`, `@PATCH`, `@DELETE` — HTTP методы
- `@Query` — параметр в URL (например, `?namePrefix=Москва`)
- `@Path` — часть пути URL (например, `/checklist/{slug}`)
- `@Body` — тело запроса (JSON объект)
- `suspend` — функция корутины (асинхронная)

---

### 4. data/repository/LuggifyRepository.kt

**Назначение:** Слой абстракции между ViewModel и API. Обрабатывает ошибки и возвращает `Result<T>`.

#### Структура:

```kotlin
// Repository - слой абстракции между ViewModel и API
// Скрывает детали HTTP-запросов от ViewModel
class LuggifyRepository {
    // Получаем экземпляр API из ApiClient
    private val api = ApiClient.api
    
    // Универсальная функция для обработки всех API-вызовов
    // <T> - generic тип (может быть любой тип данных)
    // call - функция, которая выполняет API-запрос
    // errorMessage - сообщение для ошибки
    private suspend fun <T> handleApiCall(
        call: suspend () -> Response<T>,  // Функция, возвращающая Response
        errorMessage: String              // Базовое сообщение об ошибке
    ): Result<T> {                        // Result - успех или ошибка
        return try {
            // Выполняем API-запрос
            val response = call()
            
            // Проверяем результат
            when {
                // Успешный ответ (код 200-299) и есть тело ответа
                response.isSuccessful && response.body() != null -> {
                    // Возвращаем успешный результат с данными
                    // !! - утверждаем, что body не null (мы уже проверили выше)
                    Result.success(response.body()!!)
                }
                // Ошибка от сервера (код 400, 500 и т.д.)
                else -> {
                    // Пытаемся прочитать тело ошибки
                    val errorBody = try {
                        // response.errorBody() - тело ошибки от сервера
                        response.errorBody()?.string() ?: "Неизвестная ошибка"
                    } catch (e: Exception) {
                        "Ошибка чтения ответа"
                    }
                    // Возвращаем ошибку с кодом статуса и сообщением
                    Result.failure(Exception("$errorMessage (${response.code()}): $errorBody"))
                }
            }
        } catch (e: SocketTimeoutException) {
            // Превышено время ожидания ответа от сервера
            Result.failure(Exception("Таймаут соединения. Сервер не отвечает. Попробуйте позже."))
        } catch (e: UnknownHostException) {
            // Нет интернета или сервер недоступен
            Result.failure(Exception("Не удалось подключиться к серверу. Проверьте интернет соединение."))
        } catch (e: IOException) {
            // Общая ошибка сети
            Result.failure(Exception("Ошибка сети: ${e.message}"))
        } catch (e: Exception) {
            // Любая другая ошибка
            Result.failure(Exception("$errorMessage: ${e.message}"))
        }
    }
    
    // Метод для поиска городов
    suspend fun getCities(query: String): Result<List<City>> {
        // Убираем пробелы и проверяем, что запрос не пустой
        val trimmedQuery = query.trim()
        if (trimmedQuery.isEmpty()) {
            // Возвращаем пустой список, если запрос пустой
            return Result.success(emptyList())
        }
        
        // Используем универсальную обработку
        return handleApiCall(
            call = { api.getCities(trimmedQuery) },  // Лямбда с API-вызовом
            errorMessage = "Ошибка загрузки городов"
        )
    }
    
    // Метод для генерации чеклиста
    suspend fun generatePackingList(request: PackingRequest): Result<Checklist> {
        return handleApiCall(
            call = { api.generatePackingList(request) },
            errorMessage = "Ошибка генерации списка"
        )
    }
    
    // Аналогично для других методов...
}
```

**Почему это важно:**
- Централизованная обработка ошибок
- Единый интерфейс для всех API-вызовов
- ViewModel не знает о деталях HTTP-запросов

---

### 5. data/datastore/UserPreferencesDataStore.kt

**Назначение:** Сохранение пользовательских настроек (город, даты) локально на устройстве.

#### Как работает DataStore:

```kotlin
// Создание DataStore через extension property
// Это позволяет обращаться к dataStore через context.dataStore
// by preferencesDataStore - делегирует создание DataStore библиотеке
// name - имя файла для хранения данных
private val Context.dataStore: DataStore<Preferences> by preferencesDataStore(
    name = "user_preferences"  // Файл будет: user_preferences.preferences_pb
)

// Ключи для хранения данных - это уникальные идентификаторы
// stringPreferencesKey - ключ для строковых значений
// longPreferencesKey - ключ для числовых значений (timestamp)
private val CITY_KEY = stringPreferencesKey("selected_city")      // Ключ для города
private val START_DATE_KEY = longPreferencesKey("start_date")      // Ключ для даты начала
private val END_DATE_KEY = longPreferencesKey("end_date")           // Ключ для даты окончания
```

**Методы класса:**

```kotlin
class UserPreferencesDataStore(private val context: Context) {
    // Gson - библиотека для преобразования объектов в JSON и обратно
    private val gson = Gson()
    
    // Получить сохраненный город из DataStore
    suspend fun getCity(): City? {
        // context.dataStore.data - Flow с текущими значениями
        // .first() - получает первое (текущее) значение из Flow
        val preferences = context.dataStore.data.first()
        
        // preferences[CITY_KEY] - получаем значение по ключу (может быть null)
        // ?.let - выполняет код только если значение не null
        return preferences[CITY_KEY]?.let { cityJson ->
            // fromJson - преобразует JSON строку в объект City
            // Если преобразование не удалось, вернется null
            try {
                gson.fromJson(cityJson, City::class.java)
            } catch (e: Exception) {
                null  // В случае ошибки возвращаем null
            }
        }
    }
    
    // Сохранить город в DataStore
    suspend fun saveCity(city: City) {
        // edit - атомарная операция изменения данных
        // preferences - мутабельный объект для изменения
        context.dataStore.edit { preferences ->
            // toJson - преобразует объект City в JSON строку
            // Сохраняем JSON строку по ключу CITY_KEY
            preferences[CITY_KEY] = gson.toJson(city)
        }
        // После выхода из edit данные автоматически сохраняются в файл
    }
    
    // Получить сохраненную дату начала
    suspend fun getStartDate(): Date? {
        val preferences = context.dataStore.data.first()
        // preferences[START_DATE_KEY] - получаем Long (timestamp)
        // ?.let - если значение есть, создаем Date из timestamp
        return preferences[START_DATE_KEY]?.let { Date(it) }
    }
    
    // Сохранить дату начала
    suspend fun saveStartDate(date: Date) {
        context.dataStore.edit { preferences ->
            // date.time - получаем timestamp (миллисекунды с 1970 года)
            // Сохраняем как Long
            preferences[START_DATE_KEY] = date.time
        }
    }
    
    // Аналогично для даты окончания...
}
```

**Объяснение:**
- `preferencesDataStore` — создает DataStore с именем файла
- `data.first()` — получает текущие значения (асинхронно)
- `edit { }` — изменяет значения атомарно
- `Gson` — используется для сериализации объекта `City` в JSON

---

### 6. utils/UserIdHelper.kt

**Назначение:** Получение уникального идентификатора устройства.

```kotlin
// object - singleton объект (один экземпляр на все приложение)
object UserIdHelper {
    // Функция для получения уникального ID устройства
    fun getUserId(context: Context): String {
        // Получаем ANDROID_ID через системные настройки
        // Settings.Secure - защищенные системные настройки
        // ANDROID_ID - уникальный ID для каждого устройства (64-битное число в hex)
        return Settings.Secure.getString(
            context.contentResolver,      // ContentResolver - доступ к системным данным
            Settings.Secure.ANDROID_ID     // Константа для ANDROID_ID
        ) ?: "android_user_${System.currentTimeMillis()}"
        // ?: - если ANDROID_ID недоступен (null), создаем временный ID
        // System.currentTimeMillis() - текущее время в миллисекундах
        // Это гарантирует уникальность временного ID
    }
}
```

**Объяснение:**
- `ANDROID_ID` — уникальный ID для каждого устройства
- Если ID недоступен, генерируется временный ID на основе времени

---

### 7. ui/viewmodel/LuggifyViewModel.kt

**Назначение:** Управление состоянием UI и бизнес-логикой.

#### UiState — состояние UI:

```kotlin
data class UiState(
    val isLoading: Boolean = false,              // Загрузка генерации чеклиста
    val isLoadingCities: Boolean = false,        // Загрузка списка городов
    val isLoadingChecklists: Boolean = false,    // Загрузка сохраненных чеклистов
    val error: String? = null,                  // Сообщение об ошибке
    val selectedCity: City? = null,              // Выбранный город
    val startDate: Date? = null,                // Дата начала
    val endDate: Date? = null,                  // Дата окончания
    val checklist: Checklist? = null,           // Текущий чеклист
    val checkedItems: Set<String> = emptySet(), // Отмеченные вещи
    val removedItems: Set<String> = emptySet(),  // Удаленные вещи
    val addedItems: List<String> = emptyList(), // Добавленные вещи
    val cities: List<City> = emptyList(),       // Список городов для автодополнения
    val myChecklists: List<Checklist> = emptyList(), // Сохраненные чеклисты
    val showAddItemDialog: Boolean = false,      // Показать диалог добавления
    val newItemText: String = "",               // Текст новой вещи
    val isDirty: Boolean = false,                // Есть несохраненные изменения
    val saveStateSuccess: Boolean = false,       // Успешное сохранение состояния
    val saveSuccess: Boolean = false,           // Успешное сохранение чеклиста
    val isFromMyChecklists: Boolean = false      // Чеклист из "Мои чеклисты"
)
```

#### Основные методы ViewModel:

**1. `loadSavedPreferences()`** — загрузка сохраненных настроек:
```kotlin
private fun loadSavedPreferences() {
    // Проверяем, что DataStore инициализирован (может быть null)
    // ?.let - выполняем код только если dataStore не null
    userPreferencesDataStore?.let { dataStore ->
        // viewModelScope.launch - запускаем корутину в контексте ViewModel
        // Корутина автоматически отменяется при уничтожении ViewModel
        viewModelScope.launch {
            // Асинхронно загружаем сохраненные данные
            // suspend функции выполняются последовательно
            val savedCity = dataStore.getCity()          // Загружаем город
            val savedStartDate = dataStore.getStartDate() // Загружаем дату начала
            val savedEndDate = dataStore.getEndDate()     // Загружаем дату окончания
            
            // Обновляем состояние UI с сохраненными значениями
            // _uiState.value - текущее состояние
            // .copy() - создает копию состояния с новыми значениями
            // Это безопасный способ обновления immutable data class
            _uiState.value = _uiState.value.copy(
                selectedCity = savedCity,    // Устанавливаем сохраненный город
                startDate = savedStartDate,  // Устанавливаем сохраненную дату начала
                endDate = savedEndDate       // Устанавливаем сохраненную дату окончания
            )
        }
    }
}
```

**2. `searchCities(query: String)`** — поиск городов:
```kotlin
fun searchCities(query: String) {
    // Если запрос пустой, очищаем список городов
    if (query.isEmpty()) {
        _uiState.value = _uiState.value.copy(
            cities = emptyList(),         // Очищаем список
            isLoadingCities = false       // Убираем индикатор загрузки
        )
        return  // Выходим из функции раньше
    }
    
    // Запускаем корутину для асинхронного запроса
    viewModelScope.launch {
        // Показываем индикатор загрузки
        _uiState.value = _uiState.value.copy(isLoadingCities = true)
        
        // Выполняем запрос к API через Repository
        // repository.getCities() возвращает Result<List<City>>
        // fold() - метод для обработки успеха и ошибки одновременно
        repository.getCities(query).fold(
            // onSuccess - если запрос успешен
            onSuccess = { cities ->
                // Обновляем состояние с найденными городами
                _uiState.value = _uiState.value.copy(
                    cities = cities,              // Список найденных городов
                    isLoadingCities = false,      // Скрываем индикатор загрузки
                    error = null                  // Очищаем ошибки (если были)
                )
            },
            // onFailure - если произошла ошибка
            onFailure = { error ->
                // Обновляем состояние с ошибкой
                _uiState.value = _uiState.value.copy(
                    error = error.message,        // Сообщение об ошибке
                    cities = emptyList(),         // Очищаем список
                    isLoadingCities = false       // Скрываем индикатор загрузки
                )
            }
        )
    }
}
```

**3. `generatePackingList()`** — генерация чеклиста:
```kotlin
fun generatePackingList() {
    // Получаем текущие значения из состояния
    val selectedCity = _uiState.value.selectedCity  // Выбранный город
    val startDate = _uiState.value.startDate        // Дата начала
    val endDate = _uiState.value.endDate            // Дата окончания

    // Валидация: проверяем, что все поля заполнены
    if (selectedCity == null || startDate == null || endDate == null) {
        // Если какое-то поле не заполнено, показываем ошибку
        _uiState.value = _uiState.value.copy(error = "Заполните все поля!")
        return  // Выходим из функции
    }

    // Создаем форматтер для даты (формат: год-месяц-день)
    // Locale.getDefault() - использует локаль устройства (русский/английский и т.д.)
    val dateFormat = SimpleDateFormat("yyyy-MM-dd", Locale.getDefault())
    
    // Создаем объект запроса для API
    val request = PackingRequest(
        city = selectedCity.fullName,                    // Полное название города
        start_date = dateFormat.format(startDate),      // Форматируем дату начала в строку
        end_date = dateFormat.format(endDate)           // Форматируем дату окончания в строку
    )

    // Запускаем корутину для асинхронного запроса
    viewModelScope.launch {
        // Показываем индикатор загрузки и очищаем предыдущие ошибки
        _uiState.value = _uiState.value.copy(isLoading = true, error = null)
        
        // Выполняем запрос на генерацию чеклиста
        repository.generatePackingList(request).fold(
            // onSuccess - если чеклист успешно сгенерирован
            onSuccess = { checklist ->
                // Обновляем состояние с новым чеклистом
                _uiState.value = _uiState.value.copy(
                    isLoading = false,           // Скрываем индикатор загрузки
                    checklist = checklist,       // Сохраняем сгенерированный чеклист
                    checkedItems = emptySet(),   // Очищаем отмеченные вещи (новый чеклист)
                    removedItems = emptySet(),   // Очищаем удаленные вещи
                    addedItems = emptyList()     // Очищаем добавленные вещи
                )
            },
            // onFailure - если произошла ошибка
            onFailure = { error ->
                // Обновляем состояние с ошибкой
                _uiState.value = _uiState.value.copy(
                    isLoading = false,           // Скрываем индикатор загрузки
                    error = error.message        // Показываем сообщение об ошибке
                )
            }
        )
    }
}
```

**4. `toggleItemChecked(item: String)`** — переключение отметки вещи:
```kotlin
fun toggleItemChecked(item: String) {
    // Получаем текущий список отмеченных вещей
    // toMutableSet() - создает изменяемую копию Set
    val currentChecked = _uiState.value.checkedItems.toMutableSet()
    
    // Проверяем, отмечена ли вещь
    if (item in currentChecked) {
        // Если отмечена - снимаем отметку (удаляем из Set)
        currentChecked.remove(item)
    } else {
        // Если не отмечена - ставим отметку (добавляем в Set)
        currentChecked.add(item)
    }
    
    // Обновляем состояние
    _uiState.value = _uiState.value.copy(
        checkedItems = currentChecked,  // Новый список отмеченных вещей
        isDirty = true                  // Помечаем, что есть несохраненные изменения
    )
}
```

**5. `removeItem(item: String)`** — удаление вещи:
```kotlin
fun removeItem(item: String) {
    // Получаем текущий список отмеченных вещей
    val currentChecked = _uiState.value.checkedItems.toMutableSet()
    // Снимаем отметку с удаляемой вещи (если была отмечена)
    currentChecked.remove(item)
    
    // Получаем список вещей, добавленных пользователем
    val currentAddedItems = _uiState.value.addedItems.toMutableList()
    // Проверяем, добавлена ли эта вещь пользователем
    val isAddedByUser = currentAddedItems.contains(item)
    
    if (isAddedByUser) {
        // Если вещь была добавлена пользователем - удаляем её из списка добавленных
        currentAddedItems.remove(item)
        _uiState.value = _uiState.value.copy(
            addedItems = currentAddedItems,  // Обновляем список добавленных (без этой вещи)
            checkedItems = currentChecked,    // Обновляем список отмеченных
            isDirty = true                    // Помечаем несохраненные изменения
        )
    } else {
        // Если это оригинальная вещь из чеклиста - добавляем в список удаленных
        // Это нужно, чтобы при сохранении сервер знал, какие вещи удалить
        val currentRemoved = _uiState.value.removedItems.toMutableSet()
        currentRemoved.add(item)
        _uiState.value = _uiState.value.copy(
            removedItems = currentRemoved,    // Обновляем список удаленных (добавили эту вещь)
            checkedItems = currentChecked,    // Обновляем список отмеченных
            isDirty = true                    // Помечаем несохраненные изменения
        )
    }
}
```

**Важные моменты:**
- `MutableStateFlow` — хранит состояние UI
- `asStateFlow()` — преобразует в `StateFlow` (только для чтения)
- `viewModelScope.launch` — запускает корутину, которая отменяется при уничтожении ViewModel
- `Result.fold()` — удобный способ обработки успеха/ошибки

---

### 8. ui/screens/ — Экраны приложения

#### HomeScreen.kt

**Назначение:** Главный экран с выбором города, дат и генерацией чеклиста.

**Структура:**
```kotlin
// @Composable - помечает функцию как Compose UI
@Composable
fun HomeScreen(
    viewModel: LuggifyViewModel,              // ViewModel для доступа к данным
    onNavigateToChecklist: (String) -> Unit,  // Callback для перехода на чеклист (slug)
    onNavigateToMyChecklists: () -> Unit      // Callback для перехода на "Мои чеклисты"
) {
    // Получаем состояние из ViewModel
    // by - делегирование свойства
    // collectAsState() - подписывается на изменения StateFlow и преобразует в State
    // При изменении состояния UI автоматически перерисовывается
    val uiState by viewModel.uiState.collectAsState()
    
    // LaunchedEffect - выполняет побочный эффект при изменении ключа
    // uiState.checklist?.slug - ключ эффекта (если slug изменится, эффект запустится)
    LaunchedEffect(uiState.checklist?.slug) {
        // Проверяем, что slug не null
        uiState.checklist?.slug?.let { slug ->
            // Переходим на экран чеклиста с полученным slug
            onNavigateToChecklist(slug)
        }
    }
    
    // Column - вертикальный контейнер (аналог LinearLayout с вертикальной ориентацией)
    Column(
        modifier = Modifier
            .fillMaxSize()                      // Занимает весь доступный размер
            .verticalScroll(scrollState)        // Прокручиваемый контент
            .padding(16.dp),                    // Отступы со всех сторон
        horizontalAlignment = Alignment.CenterHorizontally,  // Выравнивание по центру
        verticalArrangement = Arrangement.spacedBy(16.dp)     // Расстояние между элементами
    ) {
        // Логотип и название приложения
        Column(
            horizontalAlignment = Alignment.CenterHorizontally,
            verticalArrangement = Arrangement.spacedBy(8.dp),
            modifier = Modifier.padding(vertical = 24.dp)
        ) {
            Image(
                painter = painterResource(id = R.drawable.ic_luggify_logo),
                contentDescription = "Luggify Logo",
                modifier = Modifier.size(80.dp),
                colorFilter = ColorFilter.tint(MaterialTheme.colorScheme.primary)
            )
            Text(
                text = "Luggify",
                style = MaterialTheme.typography.headlineLarge,
                color = MaterialTheme.colorScheme.primary
            )
        }
        
        // Компонент выбора города
        CitySelect(
            selectedCity = uiState.selectedCity,      // Текущий выбранный город
            cities = uiState.cities,                  // Список найденных городов
            isLoading = uiState.isLoadingCities,      // Индикатор загрузки
            onCitySelected = { viewModel.selectCity(it) },      // При выборе города
            onCityCleared = { viewModel.clearCity() },          // При очистке
            onSearchQueryChanged = { viewModel.searchCities(it) }, // При вводе текста
            modifier = Modifier.fillMaxWidth()
        )
        
        // Компонент выбора дат
        DateRangePicker(
            startDate = uiState.startDate,            // Дата начала
            endDate = uiState.endDate,                // Дата окончания
            onStartDateSelected = { viewModel.setStartDate(it) },  // При выборе даты начала
            onEndDateSelected = { viewModel.setEndDate(it) },     // При выборе даты окончания
            modifier = Modifier.fillMaxWidth()
        )
        
        Spacer(modifier = Modifier.height(8.dp))  // Отступ между элементами
        
        // Кнопка генерации чеклиста
        Button(
            onClick = { viewModel.generatePackingList() },  // При нажатии запускаем генерацию
            // Кнопка активна только если:
            // - не идет загрузка (!uiState.isLoading)
            // - выбран город (uiState.selectedCity != null)
            // - выбрана дата начала (uiState.startDate != null)
            // - выбрана дата окончания (uiState.endDate != null)
            enabled = !uiState.isLoading && 
                     uiState.selectedCity != null && 
                     uiState.startDate != null && 
                     uiState.endDate != null,
            modifier = Modifier
                .fillMaxWidth()                      // На всю ширину
                .padding(vertical = 8.dp),          // Вертикальные отступы
            colors = ButtonDefaults.buttonColors(
                containerColor = MaterialTheme.colorScheme.primary,
                contentColor = MaterialTheme.colorScheme.onPrimary
            )
        ) {
            // Если идет загрузка, показываем индикатор
            if (uiState.isLoading) {
                CircularProgressIndicator(
                    modifier = Modifier.size(20.dp),
                    color = MaterialTheme.colorScheme.onPrimary
                )
                Spacer(modifier = Modifier.width(8.dp))
            }
            Text("Сгенерировать список")
        }
        
        // Кнопка "Мои чеклисты"
        OutlinedButton(
            onClick = { onNavigateToMyChecklists() },  // Переход на экран "Мои чеклисты"
            modifier = Modifier
                .fillMaxWidth()
                .padding(vertical = 8.dp),
            colors = ButtonDefaults.outlinedButtonColors(
                contentColor = MaterialTheme.colorScheme.primary
            )
        ) {
            Text("Мои чеклисты")
        }
        
        // Отображение ошибок (если есть)
        // ?.let - выполняет код только если error не null
        uiState.error?.let { error ->
            Spacer(modifier = Modifier.height(8.dp))
            Card(
                modifier = Modifier.fillMaxWidth(),
                colors = CardDefaults.cardColors(
                    containerColor = MaterialTheme.colorScheme.errorContainer
                )
            ) {
                Text(
                    text = error,
                    modifier = Modifier.padding(16.dp),
                    color = MaterialTheme.colorScheme.onErrorContainer,
                    textAlign = TextAlign.Center
                )
            }
        }
    }
}
```

**Объяснение:**
- `collectAsState()` — подписывается на изменения состояния
- `LaunchedEffect` — выполняет действие при изменении `uiState.checklist?.slug`
- Кнопка генерации активна только когда все поля заполнены

#### ChecklistScreen.kt

**Назначение:** Экран просмотра и редактирования чеклиста.

**Основные функции:**
- Отображение списка вещей с чекбоксами
- Отметка выполненных вещей
- Удаление вещей
- Добавление новых вещей
- Сохранение изменений
- Отображение прогноза погоды

**Ключевые моменты:**
```kotlin
// Загрузка чеклиста при открытии экрана
// LaunchedEffect - выполняет код при изменении ключа (slug)
// slug - уникальный идентификатор чеклиста из URL
LaunchedEffect(slug) {
    // Проверяем, что текущий загруженный чеклист не соответствует slug
    // Это предотвращает повторную загрузку, если чеклист уже загружен
    if (uiState.checklist?.slug != slug) {
        // Загружаем чеклист по slug из API через ViewModel
        viewModel.loadChecklist(slug)
    }
}

// Фильтрация видимых элементов чеклиста
// visibleItems - список вещей, которые должны отображаться на экране
val visibleItems = checklist.items
    // .filter - оставляем только элементы, которые НЕ в списке удаленных
    // !uiState.removedItems.contains(it) - проверяем, что элемент не удален
    // it - текущий элемент из списка items
    .filter { !uiState.removedItems.contains(it) }
    // .plus - добавляем к отфильтрованному списку вещи, добавленные пользователем
    // Это позволяет показать оригинальные вещи (без удаленных) + добавленные пользователем
    .plus(uiState.addedItems)
```

#### MyChecklistsScreen.kt

**Назначение:** Экран со списком сохраненных чеклистов.

**Функции:**
- Отображение списка сохраненных чеклистов
- Открытие чеклиста
- Удаление чеклиста

---

### 9. ui/components/ — Переиспользуемые компоненты

#### CitySelect.kt

**Назначение:** Компонент выбора города с автодополнением.

**Как работает:**
1. Пользователь вводит текст в `OutlinedTextField`
2. Через 300мс (debounce) отправляется запрос на поиск
3. Результаты отображаются в выпадающем списке
4. При выборе города вызывается `onCitySelected`

**Важные части:**
```kotlin
// Debounce для поиска - предотвращает лишние запросы при быстром вводе
// LaunchedEffect запускается при изменении searchQuery
LaunchedEffect(searchQuery) {
    // Проверяем, что текст отличается от выбранного города и не пустой
    if (searchQuery != selectedCity?.fullName && searchQuery.isNotEmpty()) {
        // delay - приостанавливает выполнение корутины на 300мс
        // Это дает время пользователю закончить ввод
        delay(300) // Задержка 300 миллисекунд
        
        // После задержки проверяем, что текст все еще не пустой
        // (пользователь мог очистить поле за это время)
        if (searchQuery.isNotEmpty()) {
            // Вызываем callback для поиска городов
            // Это вызовет viewModel.searchCities() в HomeScreen
            onSearchQueryChanged(searchQuery)
        }
    }
}
```

#### DateRangePicker.kt

**Назначение:** Компонент выбора даты начала и окончания поездки.

**Функции:**
- Два поля для дат
- Валидация: нельзя выбрать дату раньше сегодня
- Дата окончания не может быть раньше даты начала
- Календарный выбор через Material3 DatePicker

**Важные моменты:**
```kotlin
// Валидация даты начала при выборе в календаре
// date.time - timestamp выбранной даты в миллисекундах
// today - timestamp сегодняшней даты (время установлено на 00:00:00)
if (date.time >= today) {
    // Если дата сегодня или в будущем - сохраняем
    selectedStartDate = date                // Сохраняем выбранную дату локально
    onStartDateSelected(date)               // Отправляем в ViewModel
    showStartDatePicker = false             // Закрываем календарь
    focusManager.clearFocus()               // Убираем фокус с поля ввода
} else {
    // Дата в прошлом - не сохраняем, просто закрываем календарь
    showStartDatePicker = false
}

// Минимальная дата окончания - дата начала (или сегодня, если дата начала не выбрана)
// Это нужно, чтобы дата окончания не могла быть раньше даты начала
val minEndDate = if (selectedStartDate.time >= today) {
    selectedStartDate.time  // Если дата начала выбрана - используем её
} else {
    today                    // Если дата начала не выбрана - используем сегодняшнюю дату
}
```

#### ChecklistItem.kt

**Назначение:** Один элемент чеклиста с чекбоксом и кнопкой удаления.

**Структура:**
```kotlin
@Composable
fun ChecklistItem(
    item: String,              // Название вещи
    isChecked: Boolean,         // Отмечена ли вещь
    onCheckedChange: () -> Unit,  // Callback при изменении отметки
    onRemove: () -> Unit,        // Callback при удалении
    modifier: Modifier = Modifier
) {
    // Surface - контейнер с фоном и тенями
    Surface(
        modifier = modifier.fillMaxWidth(),  // На всю ширину родителя
        color = MaterialTheme.colorScheme.surface,  // Цвет фона
        shape = MaterialTheme.shapes.small     // Скругленные углы
    ) {
        // Row - горизонтальный контейнер
        Row(
            modifier = Modifier
                .fillMaxWidth()
                .padding(horizontal = 12.dp, vertical = 10.dp),  // Внутренние отступы
            verticalAlignment = Alignment.CenterVertically,  // Выравнивание по центру
            horizontalArrangement = Arrangement.spacedBy(12.dp)  // Расстояние между элементами
        ) {
            // Чекбокс для отметки вещи
            Checkbox(
                checked = isChecked,  // Текущее состояние (отмечена/нет)
                onCheckedChange = { onCheckedChange() },  // При изменении состояния
                colors = CheckboxDefaults.colors(
                    checkedColor = MaterialTheme.colorScheme.primary,
                    uncheckedColor = MaterialTheme.colorScheme.outline
                ),
                modifier = Modifier.size(20.dp)  // Размер чекбокса
            )
            
            // Текст с названием вещи
            Text(
                text = item,
                modifier = Modifier
                    .weight(1f)  // Занимает все доступное пространство
                    .clickable(  // Можно кликнуть на текст для отметки
                        onClick = { onCheckedChange() },
                        indication = null,  // Убираем ripple эффект (уже есть на чекбоксе)
                        interactionSource = remember { MutableInteractionSource() }
                    ),
                style = MaterialTheme.typography.bodyLarge.copy(
                    // Если отмечена - делаем текст полупрозрачным
                    color = if (isChecked) {
                        MaterialTheme.colorScheme.primary.copy(alpha = 0.7f)
                    } else {
                        MaterialTheme.colorScheme.onSurface
                    }
                )
            )
            
            Spacer(modifier = Modifier.width(4.dp))  // Небольшой отступ
            
            // Кнопка удаления
            IconButton(
                onClick = onRemove,  // При нажатии вызываем callback удаления
                modifier = Modifier.size(32.dp)
            ) {
                Icon(
                    imageVector = Icons.Default.Close,  // Иконка крестика
                    contentDescription = "Удалить",     // Для accessibility
                    modifier = Modifier.size(18.dp),
                    tint = MaterialTheme.colorScheme.error.copy(alpha = 0.6f)  // Красный цвет
                )
            }
        }
    }
}
```

#### WeatherForecast.kt

**Назначение:** Отображение прогноза погоды на дни поездки.

**Структура:**
- Горизонтальный список карточек
- Каждая карточка: дата, иконка погоды, описание, температура

---

### 10. ui/theme/ — Тема приложения

#### Color.kt

Определяет цветовую схему:
```kotlin
// Color - цвет в формате ARGB (Alpha, Red, Green, Blue)
// 0xFFFF9800 - оранжевый цвет в hex формате
// FF - альфа канал (полная непрозрачность), FF9800 - RGB (красный, зеленый, синий)
val Primary = Color(0xFFFF9800)          // Основной цвет (оранжевый) - используется для кнопок, акцентов
val PrimaryVariant = Color(0xFFFFAE42) // Вариант основного цвета (светло-оранжевый) - используется для границ
val Secondary = Color(0xFF757575)        // Вторичный цвет (серый) - используется для второстепенных элементов
val Background = Color(0xFF1A1A1A)         // Цвет фона (темный) - используется для основного фона экрана
val Surface = Color(0xFF2B2B2B)           // Цвет поверхности (темно-серый) - используется для карточек, панелей
val Error = Color(0xFFB00020)             // Цвет ошибки (красный) - используется для сообщений об ошибках
val OnPrimary = Color(0xFF000000)         // Цвет текста на основном цвете (черный) - для текста на оранжевых кнопках
val OnSecondary = Color(0xFFFFFFFF)       // Цвет текста на вторичном цвете (белый)
val OnBackground = Color(0xFFFFFFFF)      // Цвет текста на фоне (белый) - основной цвет текста
val OnSurface = Color(0xFFFFFFFF)         // Цвет текста на поверхности (белый) - для текста на карточках
val OnError = Color(0xFFFFFFFF)           // Цвет текста на ошибке (белый) - для текста в сообщениях об ошибках
```

#### Theme.kt

Настраивает тему Material3 и статус-бар:
```kotlin
@Composable
fun LuggifyTheme(content: @Composable () -> Unit) {
    // Выбираем цветовую схему (темная тема)
    val colorScheme = DarkColorScheme
    // LocalView - получаем текущий View из контекста Compose
    val view = LocalView.current
    
    // Настройка статус-бара (только если не в режиме предпросмотра)
    if (!view.isInEditMode) {
        // SideEffect - выполняет побочный эффект при каждой рекомпозиции
        SideEffect {
            // Получаем окно Activity
            val window = (view.context as Activity).window
            // Устанавливаем цвет статус-бара (оранжевый)
            // toArgb() - преобразует Color в ARGB (Alpha, Red, Green, Blue)
            window.statusBarColor = colorScheme.primary.toArgb()
            
            // Настройка для Android M (API 23) и выше
            // Светлый текст на темном фоне статус-бара
            @Suppress("DEPRECATION")  // Подавляем предупреждение о deprecated API
            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.M) {
                // Получаем текущие флаги видимости UI
                var flags = window.decorView.systemUiVisibility
                // Добавляем флаг для светлого текста на статус-баре
                // or - побитовое ИЛИ (добавляет флаг, не удаляя существующие)
                flags = flags or android.view.View.SYSTEM_UI_FLAG_LIGHT_STATUS_BAR
                // Применяем новые флаги
                window.decorView.systemUiVisibility = flags
            }
        }
    }
    
    // Применяем Material3 тему ко всему содержимому
    MaterialTheme(
        colorScheme = colorScheme,  // Цветовая схема
        typography = Typography,    // Типографика (шрифты, размеры)
        content = content          // Содержимое, к которому применяется тема
    )
}
```

#### Type.kt

Определяет типографику:
```kotlin
// Typography - настройки текста для всего приложения
// Используется Material3 Typography для единообразного стиля
val Typography = Typography(
    // Стиль для основного текста (большой размер)
    bodyLarge = TextStyle(
        fontFamily = FontFamily.Default,  // Стандартный шрифт системы
        fontWeight = FontWeight.Normal,    // Обычная толщина (не жирный)
        fontSize = 16.sp,                  // Размер шрифта 16sp
        lineHeight = 24.sp,                // Высота строки 24sp
        letterSpacing = 0.5.sp             // Расстояние между буквами 0.5sp
    ),
    // Стиль для заголовков (крупный размер)
    titleLarge = TextStyle(
        fontFamily = FontFamily.Default,
        fontWeight = FontWeight.Bold,      // Жирный шрифт
        fontSize = 22.sp,                  // Размер шрифта 22sp
        lineHeight = 28.sp,                // Высота строки 28sp
        letterSpacing = 0.sp               // Без дополнительного расстояния между буквами
    ),
    // Стиль для маленьких меток
    labelSmall = TextStyle(
        fontFamily = FontFamily.Default,
        fontWeight = FontWeight.Medium,    // Средняя толщина
        fontSize = 11.sp,                  // Размер шрифта 11sp
        lineHeight = 16.sp,                // Высота строки 16sp
        letterSpacing = 0.5.sp             // Расстояние между буквами 0.5sp
    )
)
```

---

## Поток данных

### 1. Генерация чеклиста:

```
HomeScreen
  ↓ (пользователь нажимает "Сгенерировать")
LuggifyViewModel.generatePackingList()
  ↓ (создает PackingRequest)
LuggifyRepository.generatePackingList()
  ↓ (HTTP POST запрос)
LuggifyApi.generatePackingList()
  ↓ (ответ от сервера)
LuggifyRepository возвращает Result<Checklist>
  ↓ (успех)
LuggifyViewModel обновляет uiState.checklist
  ↓ (изменение состояния)
HomeScreen получает новое состояние через collectAsState()
  ↓ (LaunchedEffect срабатывает)
Автоматический переход на ChecklistScreen
```

### 2. Поиск городов:

```
CitySelect (пользователь вводит текст)
  ↓ (через 300мс debounce)
LuggifyViewModel.searchCities()
  ↓ (HTTP GET запрос)
LuggifyRepository.getCities()
  ↓ (ответ)
LuggifyViewModel обновляет uiState.cities
  ↓
CitySelect отображает список городов
```

### 3. Сохранение настроек:

```
LuggifyViewModel.selectCity()
  ↓
UserPreferencesDataStore.saveCity()
  ↓ (асинхронно)
DataStore сохраняет в файл
  ↓ (при следующем запуске)
UserPreferencesDataStore.getCity()
  ↓
LuggifyViewModel.loadSavedPreferences()
  ↓
UiState обновляется с сохраненными данными
```

---

## Технологии и библиотеки

### Основные технологии:

1. **Jetpack Compose** — современный UI фреймворк для Android
2. **Kotlin Coroutines** — асинхронное программирование
3. **StateFlow** — реактивное состояние UI
4. **Retrofit** — HTTP-клиент для работы с API
5. **Gson** — сериализация/десериализация JSON
6. **DataStore** — современная замена SharedPreferences
7. **Navigation Compose** — навигация между экранами
8. **Material3** — дизайн-система Google

### Ключевые концепции:

- **MVVM** — разделение UI и бизнес-логики
- **Reactive Programming** — UI реагирует на изменения состояния
- **Suspend Functions** — асинхронные операции без блокировки UI
- **State Management** — централизованное управление состоянием

---

## Вопросы для защиты

### 1. Что делает ваше приложение?
**Ответ:** Luggify — это приложение для генерации чеклистов вещей для путешествий. Пользователь выбирает город и даты поездки, после чего приложение генерирует список вещей на основе прогноза погоды и характеристик города.

### 2. Какая архитектура используется?
**Ответ:** Приложение использует архитектуру MVVM (Model-View-ViewModel). 
- **Model** — это данные (models, repository, API)
- **View** — это UI (Compose screens и components)
- **ViewModel** — управляет состоянием и связывает View и Model

### 3. Как работает сохранение данных?
**Ответ:** Используется Jetpack DataStore для сохранения настроек пользователя (выбранный город и даты). DataStore сохраняет данные асинхронно в файл на устройстве. При запуске приложения данные автоматически загружаются.

### 4. Как обрабатываются ошибки сети?
**Ответ:** В `LuggifyRepository` есть универсальная функция `handleApiCall`, которая обрабатывает различные типы ошибок:
- Таймаут соединения
- Отсутствие интернета
- Ошибки сервера
Все ошибки возвращаются как `Result.failure()`, и ViewModel показывает их пользователю.

### 5. Что такое StateFlow и как он используется?
**Ответ:** `StateFlow` — это реактивный поток данных, который хранит текущее состояние UI. Когда состояние изменяется, все подписчики (Compose UI) автоматически получают обновление. Это позволяет UI автоматически перерисовываться при изменении данных.

### 6. Как работает навигация?
**Ответ:** Используется Navigation Compose. В `MainActivity` настроен `NavHost` с тремя маршрутами:
- `"home"` — главный экран
- `"checklist/{slug}"` — экран чеклиста с параметром slug
- `"my-checklists"` — список сохраненных чеклистов

### 7. Что такое debounce в поиске городов?
**Ответ:** Debounce — это задержка перед отправкой запроса. Когда пользователь вводит текст, запрос отправляется только через 300мс после остановки ввода. Это предотвращает лишние запросы при быстром наборе текста.

### 8. Как работает валидация дат?
**Ответ:** В `DateRangePicker` есть проверки:
- Дата начала не может быть раньше сегодняшней
- Дата окончания не может быть раньше даты начала
- При выборе даты в календаре проверка происходит перед сохранением

### 9. Что такое корутины и зачем они нужны?
**Ответ:** Корутины — это легковесные потоки для асинхронных операций. Они используются для:
- HTTP-запросов (не блокируют UI)
- Работы с DataStore (асинхронное чтение/запись)
- Задержек (debounce в поиске)

### 10. Как работает сохранение чеклиста?
**Ответ:** При нажатии "Сохранить в мои чеклисты" создается `ChecklistCreate` объект с данными чеклиста и ID пользователя. Отправляется POST запрос на сервер, который сохраняет чеклист. После успешного сохранения список "Мои чеклисты" обновляется.

---

## Дополнительные заметки

### Почему используется DataStore вместо SharedPreferences?
- DataStore работает асинхронно (не блокирует UI)
- Более безопасная работа с данными
- Поддержка Flow для реактивного программирования
- Рекомендуется Google для современных приложений

### Почему используется Result<T> вместо try-catch?
- Единый подход к обработке ошибок
- Явное разделение успеха и ошибки
- Удобный метод `fold()` для обработки обоих случаев

### Почему используется StateFlow вместо LiveData?
- StateFlow — часть Kotlin Coroutines (более современный подход)
- Лучше работает с Compose
- Более гибкий API

---

**Конец документации**

*Этот документ содержит полный разбор Android приложения Luggify. Используйте его для подготовки к защите проекта.*

